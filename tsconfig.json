{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "./", // Adicione esta linha
    "paths": {     // Adicione este bloco
      "@/*": ["src/*"]
    }
  }
}
